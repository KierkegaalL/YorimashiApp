# CLAUDE.md — ヨリマシ.app

## プロジェクト概要

**ヨリマシ.app** は、Claude Codeでの作業実況とClaudeとのチャット対話を、憑坐(よりまし)キャラクター「常世灯里(とこよ あかり)」が感情豊かに反応しながら伴走するデスクトップ常駐アプリ。開発者自身のClaude Code活用基盤(ハーネスエンジニアリング)の一環。

## ドキュメント構成(3層)

- **本ファイル(CLAUDE.md)**: ルール層。常時読み込まれる方針・制約。
- **.claude/settings.json**: フック層。hooks設定(dispatch.sh呼び出し等)。
- **docs/**: 詳細設計・データ構造・API仕様などの参照資料(下記参照ドキュメント参照)。

## Source of Truth

- 要件定義書・基本設計書は **Notionが正本**。`docs/requirements.md`・`docs/basic-design.md`はミラー。
- 詳細設計・実装に着手する前に、**必ずNotion MCPで最新版を取得し、リポジトリ側のdocs/と差分がないか確認する**こと。差分があればdocs/側を更新してから作業を始める。

## 開発ワークフロー(4フェーズ)

1. **探索**: 関連ファイル・既存実装・Notion設計書(要件定義書・基本設計書)を確認する。
2. **計画**: 変更方針をTodoWrite等で明示してから着手する。
3. **実装**
4. **検証**: 下記の検証ルールに従う。目視確認だけで完了としない。

## 検証ルール

### 対称性チェック(このプロジェクト最重要ルール)

Live2D形式とスプライトセット形式は`CharacterRenderer`インターフェースで抽象化されているが、**実装時に片方だけ直して満足しやすい**(実際に本プロジェクトで2度発生した)。

- Live2D/スプライトセットのどちらかに関わる変更をしたら、**必ずもう一方の分岐も同時に確認・修正する**。
- `grep`等で、両形式に対応する要素(例: `EMOTION_STATES`の参照箇所、`Live2DRenderer`/`SpriteSetRenderer`の実装メソッド)が実際にペアで存在するかを機械的に確認してから完了とする。
- 「Live2Dだけ直した」「スプライトセットだけ直した」というコミットメッセージは書かない。両方確認した上でコミットする。

### 構文・型チェック

- `.tsx`/`.jsx`編集後は、目視での括弧確認に頼らず `npx esbuild <file> --outfile=/dev/null` または `tsc --noEmit` で機械的に構文・型エラーを確認する。
- importの未使用・不足も定期的に確認する(手動レビューで見逃しやすい)。

### 実機能確認の制約

- ElectronアプリはGUIウィンドウを要するため、**Claude Code(このサンドボックス環境)からは`npm start`等でのライブ動作確認ができない**。
- 検証は静的解析(esbuild/tsc/lint)に留め、実際の画面確認・操作確認はユーザーが手動で行う。「動作確認済み」と自己申告しない。

## 技術スタック(確定・変更検討済み)

- Electron + TypeScript + React + Vite
- Live2D描画: PixiJS + pixi-live2d-display(Cubism 2/4/5両対応)
- 設定バリデーション: Zod
- ローカルサーバー: Node.js(http + ws)
- **Ruby / Ruby on Railsは検討の上、不採用**(FR-6の透過・常時最前面要件をNext.js/Rails単体では満たせないため。Electronは変更しない前提で確定)

## プロジェクト固有の重要な制約

- **対応OS**: macOSのみ(v1)
- **モデル形式**: Live2D(上級者向け)/スプライトセット(標準の入口・おすすめ)の2種。`CharacterRenderer`で抽象化。上限は形式を問わず合計2体。
- **モデルの正本**: `clips`(感情↔クリップ対応)は各モデルの`manifest.json`が正本。config.json側には複製しない。
- **開発用モデルの取り扱い**: ちびキャラメーカー出力モデルは開発・検証専用。**配布物には絶対に含めない**。`dev-assets/`等、`.gitignore`対象のローカル専用ディレクトリに隔離する。
- **Chat Adapterの既定値**: 常に`mock`。`real`への切替はAPI課金が発生する操作なので、既定値を誤って変更しない。
- **セキュリティ**: ローカルサーバーは127.0.0.1限定バインド、全リクエストにトークン認証必須。この制約を緩めるような変更(0.0.0.0バインド、認証スキップ等)は行わない。

## ハーネス運用に関する注意

このアプリ自体もClaude Code hooksを受信する「Code Adapter」を実装するが、これは**開発作業に使うhooks(本CLAUDE.md・`.claude/settings.json`)とは別物**。両者を混同しないこと。

- 開発用hooks: このリポジトリを開発する際にClaude Codeが発火させるフック(通常のharness運用)。
- アプリ実装のhooks受信: `dispatch.sh`経由でアプリのローカルサーバーが受け取る、**アプリの利用者側の**Claude Code hooksイベント(FR-2)。

将来的に余裕が出たら、完成した`dispatch.sh`を自分の開発環境にも向けて、「アプリに自分の開発作業を実況させながら開発する」ドッグフーディングを検討してもよい(現時点では未着手)。

## 参照ドキュメント

- `docs/requirements.md` — 要件定義書(FR-1〜FR-14、非機能要件、セキュリティ要件、権利関係)
- `docs/basic-design.md` — 基本設計書(システム構成、コンポーネント設計、データ設計、外部IF設計)
- `docs/data.md` — config.jsonスキーマ全体、manifest.json、ディレクトリ構成
- `docs/api.md` — ローカルサーバーAPI一覧、Claude Code hooks連携イベント対応表
- `docs/security.md` — トークン認証・CSP・パストラバーサル対策
- `docs/detailed-design/` — 各詳細設計(キャラクターウィンドウ、モデルマッピングUI、オンボーディング、リップシンク、Chat Adapterエラーハンドリング、感情分類ロジック、スプライトセットパイプライン)
