# constraints.md — 制約事項・検証ルール・非機能要件

## 対称性チェック（このプロジェクト最重要ルール）

Live2D形式とスプライトセット形式は`CharacterRenderer`インターフェースで抽象化されているが、**実装時に片方だけ直して満足しやすい**（実際に本プロジェクトで2度発生した）。

- Live2D/スプライトセットのどちらかに関わる変更をしたら、**必ずもう一方の分岐も同時に確認・修正する**。
- `grep`等で、両形式に対応する要素（例: `EMOTION_STATES`の参照箇所、`Live2DRenderer`/`SpriteSetRenderer`の実装メソッド）が実際にペアで存在するかを機械的に確認してから完了とする。
- 「Live2Dだけ直した」「スプライトセットだけ直した」というコミットメッセージは書かない。両方確認した上でコミットする。
- **非対称が正当な場合もある**（例: スプライトセットの生成パイプラインはLive2Dに対応物を持たない）。その場合は「なぜ非対称でよいか」をドキュメントに明記する。黙って片方だけ書くと、実装漏れと誤読される。

### フックによる補助

`.claude/hooks/symmetry-reminder.sh`（PostToolUse）が、**片方の分岐にだけ触れる変更**を検知して注意喚起する。

- 判定対象は**ファイルの中身ではなく `git diff HEAD` の変更行**。原則が「変更をしたら両方確認する」である以上、差分で判定しなければならない（ファイル全体を見る実装だと、形式非依存の編集でも両形式に言及するファイルなら必ず発火して形骸化する）。
- 片側を直した時点から、もう片側を直すまで鳴り続ける。**コミット前に両方揃える**という原則どおりの挙動。
- 両側に触れている変更、どちらにも触れない変更では鳴らない。
- `EMOTION_STATES`は**形式固有のマーカーではない**（全10状態は両形式が共有する単一の情報源）。マーカーに含めてはならない。

**フックは補助であり判断はしない。** 誤検知もあるため、必ず自分でgrepして確認する。とくに**非対称が正当なケースはフックには判定できない**ので、理由の明記は人間/Claudeの責任。

### 既知の非対称（詳細設計で検出済み・実装前に決着が必要）

| 内容 | 出典 |
|---|---|
| リップシンクはスプライトセットで原理的に不可能（事前レンダリング済みのため） | detailed-design/lipsync.md |

> **決着済み（表から除外）**: `baseResolution`の非対称（Live2Dはモデルをロードするまでウィンドウサイズが確定しない問題）は、#4で**案2**を採用して解消した。`baseResolution`を形式共通・必須フィールドへ変更し（config-schema.ts / data.md / basic-design.md 6.1 / Notion正本すべて反映済み）、ウィンドウサイズは両形式とも`baseResolution × displaySize`で決まる。detailed-design/character-window.md 論点2参照。

> **決着済み（表から除外）**: `loop`がspriteset専用のため、Live2Dで持続中にモーションが尽きた際の挙動が未定義だった問題は、**`Live2DRenderer`が`motionFinish`を購読して同じ状態を撃ち直す**ことで解消した（2026-07-27）。**責務分担はEmotionEngine=状態の寿命 / Renderer=その状態を映し続ける方法**で、契約は両形式で対称（「setStateされた状態を次のsetStateまで映し続ける」）。手段だけが素材の性質で異なる（spriteset=素材に焼き込んだ`loop`／Live2D=再発火）ため、**この非対称は正当**。実装は`src/renderer/character/renderer/motion-refire.ts`＋`Live2DRenderer.ts`、根拠はdetailed-design/lipsync.md「決着」節。

## 検証ルール

### 構文・型チェック

- `.ts`/`.tsx`/`.jsx`編集後は、目視での括弧確認に頼らず機械的に確認する。`.claude/hooks/post-edit-check.sh`（PostToolUse）が`npm run typecheck`等を自動実行する。
- 手動で確認する場合は `npx esbuild <file> --outfile=/dev/null`（構文のみ・高速）または `npm run typecheck`（型・プロジェクト全体）。
- importの未使用・不足も定期的に確認する（手動レビューで見逃しやすい）。`tsconfig`は`noUnusedLocals`/`noUnusedParameters`を有効にしてある。

### 実機能確認の制約

- ElectronアプリはGUIウィンドウを要するため、**Claude Code（このサンドボックス環境）からは`npm start`等でのライブ動作確認ができない**。
- 検証は静的解析（esbuild/tsc/lint）に留め、実際の画面確認・操作確認はユーザーが手動で行う。**「動作確認済み」と自己申告しない。**
- ただし**GUIを伴わない検証は積極的に行う**。Electronのオフスクリーン実行、モックサーバー、ライブラリの実測は可能であり、詳細設計フェーズではこれで多数の設計判断を実証した（例: `AbortSignal`なしではstreamの凍結を検知できない、macOSは画面外座標を補正しない）。**推測で設計を書かない。**

## 正本（Source of Truth）

| 対象 | 正本 | ミラー／参照 |
|---|---|---|
| 要件・基本設計 | **Notion** | `docs/requirements.md`・`docs/basic-design.md` |
| 詳細設計 | `docs/detailed-design/` | — |
| データ設計・API・セキュリティ | `docs/data.md`・`docs/api.md`・`docs/security.md` | — |
| UI | `docs/mockups/control-panel.jsx` | — |
| 感情↔クリップ対応（`clips`） | 各モデルの`manifest.json` | config.jsonには**複製しない** |

- 要件レベルの変更が必要になったら、**必ずNotion側を先に更新してから`docs/`へ反映する**。
- Notion正本自体が古くなることがある。差分を見つけたら、どちらが正しいかを判断してから直す（過去に`windowPosition`の記載漏れが実際に発生した）。

## プロジェクト固有の制約

- **対応OS**: macOSのみ（v1）。
- **モデル形式**: Live2D（上級者向け）/ スプライトセット（標準の入口・おすすめ）の2種。`CharacterRenderer`で抽象化。上限は形式を問わず合計2体。
- **開発用モデルの取り扱い**: ちびキャラメーカー出力モデルは開発・検証専用。**配布物には絶対に含めない**。`dev-assets/`等、`.gitignore`対象のローカル専用ディレクトリに隔離する。
- **Chat Adapterの既定値**: 常に`mock`。`real`への切替はAPI課金が発生する操作なので、既定値を誤って変更しない。
- **セキュリティ**: ローカルサーバーは127.0.0.1限定バインド、全リクエストにトークン認証必須。この制約を緩めるような変更（0.0.0.0バインド、認証スキップ等）は行わない。
- **アプリが自分の状態について嘘をつかない**: 失敗を隠して代替物を返さない（例: real接続の失敗時にmockの固定返答へ自動フォールバックしない）。未設定の項目は未設定と表示する。

## 非機能要件

- **パフォーマンス**: 常駐時CPUは待機1〜2%、Reaction再生中も10%未満を目安。メモリ200MB前後。無操作時は描画fpsを落とす（60→15fps目安）。
- **可用性**: キャラウィンドウ/コントロールパネルの異常はClaude Code本体の動作をブロックしない。`dispatch.sh`は常に`exit 0`。ローカルサーバーのポート競合時は自動フォールバック。
- **保守性**: アダプタパターンにより新規連携先を既存コードに手を入れず追加可能。config.jsonは`schemaVersion`でマイグレーション対応。
- **ローカライズ**: v1は日本語UIのみ。
- **プライバシー**: テレメトリ・使用状況analytics・クラッシュレポートは一切収集しない。hooksイベントログは7日保持で自動削除、パーミッション0600。

## Obsidian Vault（ハーネスの知識置き場）

`~/Documents/Obsidian Vault/開発/` に、**実測の生データ・越境知識・セッションの経緯**を置いている。

> **⚠️ ハーネスのObsidian ≠ アプリのObsidian連携機能。**
> このアプリには `config.obsidian.vaultPath` / `syncMode` というフィールドがあり（FR未定義・未決事項）、
> これは**アプリの利用者の**Vaultを指す。開発で使うVaultとは無関係。混同しないこと。
> 「開発用hooks ≠ アプリのhooks受信」と同じ構図の**3例目**。

### 大原則: Vaultは正本ではない

正本を増やすと必ず腐る。**境界の判定基準は「cloneした他人（将来の自分）が実装に必要か」**。

| | 置き場 |
|---|---|
| 必要 | **このリポジトリ**（要件・設計・決定・未決事項一覧） |
| 無くても実装できる | **Vault**（実測の生データ、越境知識、セッションの経緯） |

### 構成

| フォルダ | type | 内容 |
|---|---|---|
| `開発/計測/` | `measurement` | 実測1件=1ノート。生の数値と再現手順。**プロジェクトを越えて効く** |
| `開発/知見/` | `insight` | 複数プロジェクトで再利用できるパターン |
| `開発/日誌/` | `log` | セッションの経緯。却下した案、回り道の理由 |
| `開発/プロジェクト/ヨリマシ.app.md` | `project` | 索引（正本の複製をしない） |

### 運用ルール

- **結論をVaultに書かない。** 結論は`docs/`が正。Vaultには根拠（生データ）を置く
- **docs内の`[[ノート名]]`はObsidianでは解決しない**（docsはVault外）。あくまで**grep可能な参照記法**であり、リンクとして機能するのはVault内のノート同士だけ
- 実測したら計測ノートを足す。**想定は`verdict`に「未検証」と明記する**
- APIキー・パス等の機密をVaultに置かない

## ハーネス運用に関する注意

このアプリ自体もClaude Code hooksを受信する「Code Adapter」を実装するが、これは**開発作業に使うhooks（`.claude/settings.json`）とは別物**。両者を混同しないこと。

- **開発用hooks**: このリポジトリを開発する際にClaude Codeが発火させるフック（本ハーネス）。
- **アプリ実装のhooks受信**: `dispatch.sh`経由でアプリのローカルサーバーが受け取る、**アプリの利用者側の**Claude Code hooksイベント（FR-2）。

オンボーディング（FR-14）は利用者側プロジェクトの`.claude/settings.json`にhooks設定を案内する。**実装時にこのリポジトリ自身の`.claude/settings.json`を書き換えないよう注意する。**

将来的に余裕が出たら、完成した`dispatch.sh`を自分の開発環境にも向けて、「アプリに自分の開発作業を実況させながら開発する」ドッグフーディングを検討してもよい（現時点では未着手）。
