# model-mapping-ui.md — モデルマッピング編集画面詳細設計

**ステータス**: 未着手(モックアップ済み、実装挙動は未確定)
**対応FR**: FR-5
**関連**: data.md 2章(manifest.json)、control-panel.jsx(モックアップ)

## 背景

Control Panelのモデル管理タブに、全10状態(Mood3+Reaction7)のマッピング編集UIはモックアップ済み。ただし以下の実装挙動が未確定。

## 検討すべき論点

1. **プレビュー再生の挙動**
   - マッピング編集画面で「このモーション/クリップを試しに再生する」ボタンを押した時、どこに描画するか(Control Panel内に小さいプレビュー枠を作るか、実際のキャラクターウィンドウで再生するか)

2. **Live2Dのモーション/表情選択UI**
   - `model3.json`のGroups(モーショングループ)・`.exp3.json`(表情)をどう一覧化してドロップダウンに出すか
   - 自動検出したマッピング候補と、ユーザーの手動修正の関係(自動検出を上書きしたら記憶するか)

3. **スプライトセットのクリップ差し替え**
   - モックアップの「変更」ボタンを押した時のファイル選択方法(ネイティブファイルダイアログか、ドラッグ&ドロップ再度か)
   - 既存クリップの削除(未割当に戻す)導線

4. **自動マッピングのマッチングロジック**
   - model3.json内のグループ名・exp3.jsonファイル名から`idle`/`happy`等のキーワードで緩くマッチングする、という方針は既出だが、具体的な正規表現・優先順位は未確定

## 検討ログ

(未記入)
